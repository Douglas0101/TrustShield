# =========================
# TrustShield - requirements.txt (infra + API + treino)
# =========================
# Alvo principal: containers Python 3.11 (imagens slim).
# Observação: se instalar fora do Docker, mantenha Python 3.11 para máxima compatibilidade.

# --- CORE DE CIÊNCIA DE DADOS E ML ---
numpy
pandas>=2.2.3,<3.0
scikit-learn
scipy
joblib==1.4.2

# --- MLOPS: EXPERIMENTAÇÃO E OTIMIZAÇÃO ---
mlflow==3.3.1
optuna==3.6.1

# --- ARMAZENAMENTO E I/O ---
pyarrow==14.0.2
boto3>=1.34,<2
psycopg2-binary==2.9.9

# --- API DE INFERÊNCIA E SERVIÇOS WEB ---
fastapi==0.110.0
uvicorn[standard]==0.30.1
pydantic>=2.6,<3
requests==2.32.3
streamlit==1.37.0

# --- MONITORAMENTO / VALIDAÇÃO / ROBUSTEZ ---
prometheus-client==0.20.0
great-expectations==0.18.12
evidently>=0.4.5
circuitbreaker==2.0.0

# --- VISUALIZAÇÃO E INTERPRETABILIDADE ---
matplotlib==3.8.4
seaborn==0.13.2
plotly==5.22.0
shap==0.46.0
lime==0.2.0.1

# --- CONFIGURAÇÃO E UTILITÁRIOS ---
pyyaml==6.0.1
python-dotenv==1.0.1
jsonschema==4.21.1
click==8.1.7
tqdm==4.66.4
rich==13.7.1
dynaconf==3.2.5

# --- QUALIDADE DE CÓDIGO E TESTES ---
black==24.4.2
flake8==7.0.0
mypy==1.10.0
pre-commit==3.7.1
pytest==8.2.1
pytest-cov==5.0.0
pytest-mock==3.14.0
pytest-bdd==7.1.0
hypothesis==6.103.0

# --- OPCIONAIS (instale só se for usar) ---
# ray[tune]>=2.10          # para usar retraining com Ray Tune (alvo "ray_tune")
# modin[dask]              # para DataFrames paralelos (opcional; o código já faz fallback)
